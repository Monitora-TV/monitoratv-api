import { tb_tipo_resultado_hivib } from '@prisma/client';

export class Tbtiporesultadohivib implements tb_tipo_resultado_hivib {
    id: number;
    ds_resultado_hivib: string;
    no_filtro: string;
}
